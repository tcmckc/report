{% extends "base.html.twig" %}

{% block title %}Card{% endblock %}

{% block main %}

<h1>Card game</h1>

<ul id="cardgame-list">
    <li><i><a href="{{ path('card') }}">Landing page</a></i></li>
    <li><a href="{{ path('card-deck') }}">Get a new deck</a></li>
    <li><a href="{{ path('card-deck-shuffle') }}">Shuffle deck</a></li>
    <li><a href="{{ path('card-deck-draw') }}">Draw one card</a></li>
    <li><a href="{{ path('card-deck-draw-many', {'num': 5}) }}">Draw several cards</a></li>
</ul>

{# <form>
    <label>Draw one card: </label>
    <input type="submit" name="draw" value="Draw" 
        onClick="this.form.action='{{ path('card-deck-draw') }}';">
</form> #}

{# <form>
    <label>Draw many cards as you wish: </label>
    <input type="number" name="num_cards" min="2" max="52" value="2">
    <input type="submit" name="draw-many" value="Draw"
        onClick="this.form.action='{{ path('card-deck-draw-many', {'num': $nr}) }}';">
</form> #}


{% endblock %}
